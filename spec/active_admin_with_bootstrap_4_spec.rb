RSpec.describe ActiveAdminWithBootstrap4 do
  it "has a version number" do
    expect(ActiveAdminWithBootstrap4::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
